// The manager's own materials screen, rendered in the employee portal. See the
// drawings tab beside this one for why these are re-exports rather than copies.
export { default } from "@/app/admin/(dashboard)/projects/[id]/materials/page";
