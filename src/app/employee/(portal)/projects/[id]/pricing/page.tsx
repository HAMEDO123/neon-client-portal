// The manager's own pricing screen, rendered in the employee portal.
//
// This is the tab that shows what the studio charges its clients. The team
// seeing it was asked for and answered explicitly; it is not an oversight, and
// it should not be quietly removed by somebody who assumes otherwise.
export { default } from "@/app/admin/(dashboard)/projects/[id]/pricing/page";
